name: Authy
version: ${pluginVersion}
author: Iru21
main: me.iru.authy.Authy
api-version: ${minecraftVersion}
commands:
  authy:
    description: Information and reloading
    usage: /<command> [reload]
  register:
    description: Registers the player
    usage: /<command> [password] [repeat password]
    aliases: reg
  login:
    description: Allows the player to log in
    usage: /<command> [password]
    aliases: l
  unregister:
    description: Unregisters the player
    usage: /<command>
    aliases: unreg
  remember:
    description: Enables automatic login for the next 48h
    usage: /<command>
  pin:
    description: Enables and sets a pin code
    usage: /<command>
  changepassword:
    description: Changes player's password
    usage>: /<command> [new password] [repeat new password]